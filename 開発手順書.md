# ローカル環境の環境構築

## Volta導入

https://zenn.dev/longbridge/articles/30c70144c97d32

## node導入

```
volta install node@22
```

```
volta pin node@22
```

```
note -v
```

## 初期インストール

```
npm install
```

## .env作成

```
NOTION_API_SECRET=
DATABASE_ID=
```

## 起動

```
npm run dev
```

# ブランチ

**`main` の 1 本だけ**を使う。`main` が公開用で、Cloudflare Pages の
プロダクションブランチもこれ。`main` への push（PR のマージを含む）で
自動的にビルドされ、本番に反映される。

以前は `develop` で作業して `main` へ昇格する 2 本運用を想定していたが、
昇格が行われなくなり Cloudflare が `develop` を直接見ている状態が続いたため、
2026-09-19 に `main` へ統合した。

改修は `main` から作業ブランチを切り、PR を経由してマージする。

# マージ

Github Desktopを利用する。

## フォーク元からのマージ

「main」ブランチを選択して、「Branch」→「Merge into current branch」→「heads/upstream/main」→「Create a merge commit」

フォーク元（otoyo/astro-notion-blog）に取り込むものがあるかは次で確認できる。

```
git fetch upstream
git log --oneline main..upstream/main
```

出力が空なら取り込み済み。Astro 本体のバージョンが上がることがあるため、
取り込むときは PR を立てて、ビルド後の全ページを取り込み前と比較すること。
